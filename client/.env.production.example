# On Railway, frontend is served from Express itself (same origin).
# Leave empty — axios uses relative /api paths which just work.
# Only set this if frontend and backend are on different domains.
VITE_API_URL=
